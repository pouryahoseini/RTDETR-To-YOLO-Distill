[pytest]
testpaths = tests

# The maintained suite is the only thing pytest should collect. The one-off
# numerical probes that used to sit in the repository root under `test_` names
# now live in scratch/probes/, so this is a statement of scope rather than a
# workaround for files that only looked like tests.
